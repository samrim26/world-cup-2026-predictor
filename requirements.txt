# Vercel serverless function (api/live.py) is pure standard library — no deps.
# (Local dev for the full prediction engine still uses pyproject.toml:
#  pip install -e .  — that pulls numpy/pandas/scipy/scikit-learn/streamlit.)
